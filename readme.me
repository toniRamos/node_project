First you have to build the project with
    ./build.sh

Then you have to run it with
    compose docker

And you should have a docker with the name: 
    node_project-app-1

You can make simple requests with the following localhost route